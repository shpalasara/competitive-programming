#include<stdio.h>

int main()
{
	int n,i,j,temp,count;
	scanf("%d",&n);
	int data[n*n];
	for(i=0;i<n;i++)
	{
		for(j=0;j<n;j++)
		{
			scanf("%d",&temp);
			if(i==j)
				printf("%d ",temp);
		}
	}
	return 0;
}
